# Handoff Snapshot — 2026-07-10T18:03:03Z

```json
{
  "schema": "j-stack.handoff.blackboard.v1",
  "timestamp": "2026-07-10T18:03:03Z",
  "agent": "Claude Code (Fable 5 analysis session, Sonnet 5 default set mid-session)",
  "reason": "checkpoint",
  "repo": {
    "branch": "main",
    "head": "fa436ab184b9ed9cc899f41d1a5760bf176ed579",
    "dirty": true,
    "remote": "https://github.com/jpags77/J-Stack.git"
  },
  "state": {
    "iteration": "1",
    "phase": "EXPAND (per index.md, but stale — see conflicts)",
    "fidelity": "C — MVP (production-ready Claude Code skill stack)",
    "current_task": "Self-audit of j-stack repo: generated 10 furthering ideas + 10 redundancies/inconsistencies to prune, none yet implemented"
  },
  "claims": [
    {
      "id": "C1",
      "claim": "recall-learnings and graduate-learnings skills are referenced in ~/.claude/CLAUDE.md pipeline but do not exist as files anywhere in the j-stack repo (skills/ or agents/) — they would not survive a fresh install.sh run on another machine",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["find over repo tree showed no recall-learnings/ or graduate-learnings/ under skills/", "~/.claude/CLAUDE.md j-stack phase pipeline table"]
    },
    {
      "id": "C2",
      "claim": "The lane-ownership doctrine block (Stack ownership / Wiki / Cross-tool / Work checkpoints) is duplicated near-verbatim in 6 places: root CLAUDE.md, root AGENTS.md, .planning/CLAUDE.md, .planning/AGENTS.md, and two heredocs inside install.sh (CLAUDE_SECTION, AGENTS_SECTION/AGENTS_CODEX_APPEND) — wording has already drifted between copies",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["Read of all 4 project-level md files", "install.sh lines ~192-319"]
    },
    {
      "id": "C3",
      "claim": "install.sh clones gstack at floating HEAD via --depth 1 with no pinned SHA and no update path; its own halt message acknowledges 'gstack repo may have changed'",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["install.sh lines 69-87"]
    },
    {
      "id": "C4",
      "claim": "second-opinion is hard-wired to Codex CLI only; Gemini is named elsewhere in docs as a fallback runtime but has no review-mode wiring",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["skills/second-opinion/SKILL.md frontmatter + body", "README cross-tool sections naming Codex/Cursor/ChatGPT/Gemini"]
    },
    {
      "id": "C5",
      "claim": "tests/structure.sh and tests/codex-config.sh exist but there is no CI workflow (no .github/workflows/) running them, and no automated check that README skill tables match install.sh arrays — the 2026-06-03 log entry shows 4 such discrepancies were found by hand",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["file listing — no .github/ dir found", ".planning/log.md 2026-06-03 docs-audit entry"]
    },
    {
      "id": "C6",
      "claim": "poc-wiki-init records a fidelity target (A/B/C) in index.md but no skill's process reads or branches on that value — it is recorded but inert",
      "status": "confirmed",
      "confidence": "medium",
      "provenance": ["skills/poc-wiki-init/SKILL.md", "grep-equivalent read of session-start, qa, design-review, cso skill files — none reference fidelity conditionally"]
    },
    {
      "id": "C7",
      "claim": "j-stack deploy.md (666 lines, repo root) is the original pre-2026-06-17 implementation plan, says 'four custom skills' (now five + prior-art bundle), and is superseded by the current skills/ + install.sh structure — it sits in repo root where a fresh tool/agent could misread it as current instruction",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["Read of 'j-stack deploy.md' header", ".planning/log.md 2026-06-17 ponytail-fixes entry describing the skill-extraction refactor that superseded it"]
    },
    {
      "id": "C8",
      "claim": "untracked .codex/ directory (config.toml, hooks.json) contains machine-specific values: ANTHROPIC_BASE_URL pointing at a local headroom proxy port, and a hook command hardcoded to /opt/homebrew/bin/headroom with profile init-j-stack-ceeb93df — looks like local headroom-installer leakage, not intended for commit, and .codex/ is not in .gitignore",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["git status showed '?? .codex/'", "Read of .codex/config.toml and .codex/hooks.json"]
    },
    {
      "id": "C9",
      "claim": "Working tree has two uncommitted, coherent, ready-to-commit changes: README.md GitHub-integration section (+24 lines) and its matching tests/codex-config.sh assertion (+1 line)",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["git diff --stat", "git diff README.md", "git diff tests/codex-config.sh"]
    },
    {
      "id": "C10",
      "claim": "README 'token optimization' section says 'j-stack addresses this at two levels' then enumerates three (model routing = 'first defense', headroom = 'third defense', wiki = 'second defense', in that reading order) — miscount and out-of-order labeling",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["README.md 'Token optimization and second brain' section"]
    },
    {
      "id": "C11",
      "claim": "README claims 'every skill carries an explicit model directive injected at install time' but skills/prior-art-survey/SKILL.md has no model: frontmatter field (model injection in install.sh Phase 1 only touches the 11 gstack skills, not the 5 custom skills or the bundled prior-art-survey)",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["skills/prior-art-survey/SKILL.md frontmatter (no model: line)", "install.sh Phase 1 get_gstack_model() / Phase 2 custom-skill copy loop (no model injection)"]
    },
    {
      "id": "C12",
      "claim": "install.sh's Superpowers-installed check (`claude --help 2>/dev/null | grep -q \"superpowers|brainstorm\"`) is not a reliable detection method — claude --help does not enumerate installed plugins",
      "status": "confirmed",
      "confidence": "medium",
      "provenance": ["install.sh lines 36-44"]
    },
    {
      "id": "C13",
      "claim": "The repo's own .planning/index.md violates the prior-art gate it defines: Prior-art shows pending, yet plans/2026-06-17-ponytail-fixes.md exists (PLAN ran without SURVEY completing). Also index.md Last updated (2026-06-17) is stale against log.md's latest entries (through 2026-06-22), and Phase still reads EXPAND despite months of implementation-phase log entries",
      "status": "confirmed",
      "confidence": "high",
      "provenance": [".planning/index.md Current State block", ".planning/log.md full read", ".planning/plans/2026-06-17-ponytail-fixes.md existing"]
    },
    {
      "id": "C14",
      "claim": "Two pipeline tables disagree on skill membership: ~/.claude/CLAUDE.md includes recall-learnings/graduate-learnings but omits ponytail-review spec (PLAN gate); README's pipeline includes ponytail-review spec but omits the learnings skills. Neither table is complete/authoritative on its own",
      "status": "confirmed",
      "confidence": "high",
      "provenance": ["~/.claude/CLAUDE.md j-stack phase pipeline table", "README.md pipeline + complete skill flow sections"]
    },
    {
      "id": "C15",
      "claim": "/freeze and /guard are both installed gstack skills; README's own description states guard = freeze + careful mode (a strict superset), making /freeze redundant unless a freeze-only use case is identified",
      "status": "confirmed",
      "confidence": "medium",
      "provenance": ["README.md gstack safety skills table"]
    },
    {
      "id": "C16",
      "claim": "/codex is installed as a standalone gstack skill solely to be shelled out to by second-opinion and 'not invoked directly' per README — a candidate for folding into second-opinion directly rather than being a separate cherry-picked skill",
      "status": "confirmed",
      "confidence": "medium",
      "provenance": ["README.md gstack skills table, /codex row"]
    }
  ],
  "decisions": [
    {
      "id": "D1",
      "decision": "Bundle recall-learnings and graduate-learnings into skills/ and install.sh (resolves Q1, confirms N6) — but the bundle must ship only the skill definitions/templates, never the user's actual Obsidian vault content or any existing learnings entries. On a fresh install, a new user's vault/learnings folder must initialize empty, not seeded from the developer's personal vault.",
      "made_by": "user",
      "date": "2026-07-10"
    },
    {
      "id": "D2",
      "decision": "Do not fold /codex into second-opinion or invest further in it as a standalone concern (deprioritizes N16). Instead, make second-opinion's reviewer pluggable across backends (codex | gemini | other), per original N14 scope — user explicitly chose 'keep it pluggable' over hardcoding a single alternate vendor.",
      "made_by": "user",
      "date": "2026-07-10"
    }
  ],
  "conflicts": [
    {
      "id": "X1",
      "description": "index.md declares Phase: EXPAND and Prior-art: pending, but log.md and plans/ show the project has been through multiple implementation/decision cycles (ponytail, graphify, headroom integrations) well past EXPAND, and a plan exists without a completed survey",
      "preferred_source": "log.md (append-only activity record)",
      "reason": "log.md entries are dated and sequential; index.md's Current State block was evidently not updated after the 2026-06-17 session and is stale"
    },
    {
      "id": "X2",
      "description": "~/.claude/CLAUDE.md and README.md each present a 'the' pipeline table with different skill membership (learnings skills vs. ponytail-review spec)",
      "preferred_source": "neither — needs reconciliation",
      "reason": "Both were edited independently over time (headroom/ponytail/learnings integrations landed in separate sessions) without a single source of truth for the doctrine, which is itself finding C2"
    }
  ],
  "open_questions": [
    {
      "id": "Q2",
      "question": "Is .codex/ meant to be committed at all (e.g. as a template), or is it purely local machine state that should be gitignored?",
      "owner": "user"
    }
  ],
  "next_actions": [
    {"id": "N1", "action": "Commit the two ready, coherent uncommitted changes (README GitHub-integration section + matching test assertion)", "priority": "high", "blocked_by": []},
    {"id": "N2", "action": "Add .codex/ to .gitignore (or decide it should be a checked-in template with placeholders instead of machine-specific values)", "priority": "high", "blocked_by": ["Q2"]},
    {"id": "N3", "action": "Archive or delete 'j-stack deploy.md' from repo root (superseded by skills/ + install.sh structure)", "priority": "high", "blocked_by": []},
    {"id": "N4", "action": "Reconcile .planning/index.md Current State block against log.md (fix Phase, Prior-art flag, Last updated) — likely first real use of a future doctor/lint script", "priority": "high", "blocked_by": []},
    {"id": "N5", "action": "Single-source the lane-ownership doctrine (root CLAUDE.md, root AGENTS.md, .planning/CLAUDE.md, .planning/AGENTS.md, install.sh heredocs) into one template consumed everywhere", "priority": "medium", "blocked_by": []},
    {"id": "N6", "action": "Bundle recall-learnings/graduate-learnings into skills/ and install.sh Phase 2 (decided D1). Constraints: (a) ship only skill definitions/templates, never the developer's actual Obsidian vault content or existing learnings entries — audit both skill files for any hardcoded personal examples/paths before bundling; (b) a fresh install must initialize an empty vault/learnings structure for a new user, verified by a clean-machine or temp-HOME install test, not by inspection alone", "priority": "high", "blocked_by": []},
    {"id": "N7", "action": "Build a wiki-consistency check (doctor script) verifying Prior-art flag vs prior-art/ contents, Iteration Progress ticks vs reviews/ files, index Last-updated vs log.md tail", "priority": "medium", "blocked_by": ["N4"]},
    {"id": "N8", "action": "Pin gstack clone to a known-good commit SHA in install.sh; add an explicit --update path", "priority": "medium", "blocked_by": []},
    {"id": "N9", "action": "Add GitHub Actions CI running tests/structure.sh, tests/codex-config.sh, shellcheck install.sh, and a README-vs-install.sh skill-table consistency check", "priority": "medium", "blocked_by": []},
    {"id": "N10", "action": "Fix README token-optimization section: correct 'two levels' to three, renumber first/second/third to match reading order", "priority": "low", "blocked_by": []},
    {"id": "N11", "action": "Fix or soften README's 'every skill carries an explicit model directive' claim (false for prior-art-survey and other custom skills)", "priority": "low", "blocked_by": []},
    {"id": "N12", "action": "Replace install.sh's unreliable 'claude --help | grep superpowers' detection with a check against ~/.claude/plugins/ contents", "priority": "low", "blocked_by": []},
    {"id": "N13", "action": "Make fidelity target (A/B/C) actually change skill behavior in session-start / POLISH phase skills instead of being recorded and unused", "priority": "medium", "blocked_by": []},
    {"id": "N14", "action": "Make second-opinion reviewer-pluggable across backends (codex | gemini | other) instead of Codex-only (decided D2 — user explicitly chose pluggable over a single hardcoded swap)", "priority": "medium", "blocked_by": []},
    {"id": "N15", "action": "Decide whether to drop /freeze in favor of /guard alone, or document a freeze-only use case", "priority": "low", "blocked_by": []},
    {"id": "N16", "action": "DEPRIORITIZED per D2 — user said to ignore /codex and just make second-opinion pluggable (N14) instead. Leave /codex as-is; do not invest further effort here unless revisited.", "priority": "low", "blocked_by": []},
    {"id": "N17", "action": "Instrument token-per-phase-per-model logging into .planning/ (via headroom stats) and surface in stakeholder-pack", "priority": "low", "blocked_by": []},
    {"id": "N18", "action": "Publish a golden-path example .planning/ tree under examples/ showing a completed toy PoC end-to-end", "priority": "low", "blocked_by": []}
  ],
  "artifacts": [
    {"path": "README.md", "kind": "docs", "status": "modified (uncommitted, ready)"},
    {"path": "tests/codex-config.sh", "kind": "code", "status": "modified (uncommitted, ready)"},
    {"path": ".codex/", "kind": "other", "status": "referenced (untracked, needs a decision)"},
    {"path": ".planning/handoffs/2026-07-10T1803-snapshot.md", "kind": "handoff", "status": "created"}
  ]
}
```

## Human summary
Ran a full self-audit of the j-stack repo (README, install.sh, all four lane-config files, all skills/agents, uncommitted diff, and the wiki itself) and produced 10 ideas for furthering the stack's mission plus 10 redundancies/inconsistencies worth pruning. Nothing has been implemented yet — this snapshot exists purely to hand the findings to a future session as a prioritized action list.

## Freshness and source of truth
`.planning/index.md`'s Current State block (Phase: EXPAND, Prior-art: pending, Last updated: 2026-06-17) is stale — trust `.planning/log.md` instead, which runs through 2026-06-22 and shows the project well past EXPAND with multiple completed implementation cycles. Reconciling index.md is itself action item N4 below.

## Decisions
- **D1:** Bundle `recall-learnings`/`graduate-learnings` into `skills/` and `install.sh` — resolves Q1, confirms N6. Hard constraint: ship only the skill definitions/templates, never the developer's own Obsidian vault content or existing learnings entries; a fresh install must initialize an empty vault/learnings structure for a new user (verify with a clean-machine or temp-HOME install test, not just by reading the code).
- **D2:** Don't fold `/codex` into `second-opinion` or invest further in it (N16 deprioritized). Instead make `second-opinion`'s reviewer pluggable across backends (`codex | gemini | other`) per the original N14 scope — user explicitly chose "keep it pluggable" over hardcoding a single alternate vendor.

## Current task
User asked for a top-10 "furthering the mission" list and a redundancy/inconsistency prune list for the j-stack repo, then asked to persist both into a handoff file and the wiki log for later implementation sessions. In a follow-up turn the user resolved Q1 (bundle, with privacy/fresh-init constraints) and clarified the second-opinion direction (D2). Still no implementation has happened — this remains a planning artifact.

## Next steps
See `next_actions` in the JSON block above (N1–N18), ordered by priority. Highest-priority, no-decision-needed items to start with:
1. N1 — commit the two ready uncommitted changes
2. N3 — archive/delete `j-stack deploy.md`
3. N4 — reconcile `.planning/index.md` against `log.md`
4. N6 — bundle the learnings skills (now unblocked by D1, but must satisfy both constraints above)
Item N2 is still blocked on open question Q2 — ask the user before acting. N14 (pluggable second-opinion reviewer) is now unblocked per D2.

## Open questions
- **Q2:** Is `.codex/` meant to be committed as a template, or is it local machine state that should be gitignored?

## Files touched this session
- `.planning/handoffs/2026-07-10T1803-snapshot.md` — created (this file)
- `.planning/log.md` — appended (see next)
- `.planning/index.md` — will add pointer entry to this handoff

No source files were modified this session; the README/tests diff already present in the working tree predates this session and was only inspected, not authored here.

## Verification
No code was written this session, so no build/test verification applies. `git status --short` and `git diff --stat` were run to confirm which changes are pre-existing vs. new (none are new).

## Continuation prompt
Resume j-stack self-improvement work. Read `.planning/handoffs/2026-07-10T1803-snapshot.md` for full context — it contains 16 confirmed findings (C1–C16) and 18 prioritized next actions (N1–N18) from a repo self-audit. Start with N1 (commit the ready README/tests diff), N3 (archive `j-stack deploy.md`), and N4 (reconcile `.planning/index.md`'s stale Current State block against `.planning/log.md`). Before N2 and N6, ask the user Q1 and Q2 from the snapshot's open_questions. Do not silently skip the prior-art gate reconciliation (N4) — it's the same drift the gate exists to catch.
